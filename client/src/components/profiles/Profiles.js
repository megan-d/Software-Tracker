//This is the page for all of the profiles
import React from 'react';
import Wrapper from '../layout/Wrapper';

const Profiles = (props) => {
  return (
    <Wrapper>
      <p>This is the page for all of the profiles for developers</p>
    </Wrapper>
  );
};

export default Profiles;